CREATE TABLE user(
    id TEXT NOT NULL PRIMARY KEY,
    username TEXT NOT NULL,
    first_name TEXT,
    last_name TEXT,
    nickname TEXT,
    email TEXT
);

select_by_id:
SELECT *
FROM user
WHERE id IN ?;

insert_or_replace:
INSERT OR REPLACE INTO user
VALUES (?, ?, ?, ?, ?, ?);